/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // If using `public` directory
    // Or if using `src` directory:
    './src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
